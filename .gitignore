# Python
__pycache__/
*.pyc

# Selftest-Artefakte (werden bei jedem --selftest neu erzeugt)
panel_preview*.png

# Persönliches / Portfolio-Material (nicht Teil des öffentlichen Projekts)
website/

# PyInstaller-Build (die .exe gehört in GitHub-Releases, nicht ins Repo)
build/
dist/
*.spec
